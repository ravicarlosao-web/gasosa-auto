kind = "web"
previewPath = "/"
title = "Floema Site"
version = "1.0.0"
id = "artifacts/floema-site"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 5173

[services.development]
run = "pnpm --filter @workspace/floema-site run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/floema-site", "run", "build" ]
publicDir = "artifacts/floema-site/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "5173"
BASE_PATH = "/"
